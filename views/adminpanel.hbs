<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Page Title</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center">Welcome Admin</h1>
    <div class="text-center">
      <a href="/admin/adlogout" class="btn btn-primary">Logout</a>
      <a href="/admin/adduser" class="btn btn-success">Add New User</a>
    </div>

    <form action="/admin/adminpanel" method="post" class="mt-3">
      <div class="input-group">
        <input type="text" name="search" id="search" class="form-control" placeholder="Enter the name of User to be Searched">
        <div class="input-group-append">
          <button class="btn btn-success" type="submit">Search</button>
        </div>
      </div>
    </form>

    <table class="table table-bordered mt-4">
      <thead>
        <tr>
          <th>Name</th>
          <th>E-Mail</th>
          <th>Password</th>
          <th>Update</th>
          <th>Delete</th>
          <!-- Add more table headers for additional fields if needed -->
        </tr>
      </thead>
      <tbody>
        {{#each users}}
          <tr>
            <td>{{username}}</td>
            <td>{{email}}</td>
            <td>*****</td>
            <td> <a href="/admin/update/{{email}}" class="btn btn-primary">Update</a></td>
            <td> <a href="/admin/delete/{{email}}" class="btn btn-danger">Delete</a></td>
            <!-- Add more table cells for additional fields if needed -->
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
